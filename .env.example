# (Optional) If you want ALL attendance emails to go to one address, set this.
# Otherwise, the email goes to each student's own email.
# NOTIFY_EMAIL=professor@university.edu

# Server port
PORT=3000
